{
  "name": "vermogen-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate:public": "node scripts/validate-public-build.mjs",
    "check": "node --test tests/*.test.mjs && node scripts/validate-public-build.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
